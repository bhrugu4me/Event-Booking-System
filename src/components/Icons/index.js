import React from 'react';
import Loading from './loading';
import Logo from './logo';

const IconLoading = (props) => <Loading {...props} />;
const IconLogo = (props) => <Logo {...props} />;

export { IconLoading, IconLogo };
